/*
  Boolean values can be returned by simply telling the function to return
  the condition itsekf.
*/
function isLess(a, b) {
  // Fix this code
  return (a < b);
}

// Change these values to test
isLess(10, 15);
