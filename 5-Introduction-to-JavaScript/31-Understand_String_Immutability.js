/*
  When a string is created, the characters in it cannot be changed.
  The only way to change the characters is by initializing a new string for the
  variable with the corrections.
*/
// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
