/*
  The Math.random() function can return a floating point random value from 0 to 1;
*/
function randomFraction() {

  // Only change code below this line.
  return Math.random();
  // Only change code above this line.
}
