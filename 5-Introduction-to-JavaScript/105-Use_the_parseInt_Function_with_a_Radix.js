/*
  The parseInt function can also take a second argument which is the radix
  or the base of the first input value: from 2 to 36.
*/
function convertToInteger(str) {
  return parseInt(str,2);
}

convertToInteger("10011");
